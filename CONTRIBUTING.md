# 如何参与 Agent Skills 统一目录标准

本仓库是一份**开放标准提案**（草案）。参与方式分两类：厂商采纳、开发者改进。

---

## 给 Agent 厂商：如何采纳

1. 阅读 [`SPEC.md`](SPEC.md)，重点看 §2（目录路径）与 §8（一致性要求）。
2. 在你的 Agent 启动时解析技能目录：优先读取 `AGENT_SKILLS_DIR`，未设置则回退到规范路径（Windows `C:\Agent Skills\`、macOS `~/Agent Skills/`、Linux `~/.agent-skills/`）。
3. 读取 `skills.json`（或扫描子目录）发现技能，将 `name` + `description` 注入触发上下文——沿用你现有的 metadata 常驻机制即可。
4. 支持用户自定义路径（满足 §8 的 (b) 条款）。
5. 在文档 / 市场中标注「兼容 Agent Skills 统一目录标准」并链接本仓库。

> 存量技能无需迁移：可通过软链接或将技能根路径配置为 `AGENT_SKILLS_DIR` 接入，见 `SPEC.md` 附录 B。

---

## 给开发者：如何改进本标准

- 这是草案，欢迎提 **Issue** 讨论设计，提 **PR** 修改 `SPEC.md`。
- PR 请聚焦：目录约定、字段、协议的可行性与互操作性。
- 坚持**最小采纳成本**原则：优先复用各 Agent 已有的 `SKILL.md` 事实标准，不另起炉灶。
- 修改 `SPEC.md` 时，请同步更新顶部版本号（语义化版本）与「状态」说明。

---

## 示例技能提交规范

- `examples/` 下每个子目录为一个最小合规技能包。
- 必须含 `SKILL.md`，其 frontmatter 满足 [`SPEC.md` §4](SPEC.md) 的最小字段（`name`、`description` 必填）。
- 本期为提案，**不引入实现代码**；示例仅作规范锚点。

---

## 行为准则

讨论请理性、就事论事，以**互操作性**与**用户利益**为优先。不同 Agent 的实现细节可以不同，但在"统一目录"这一互操作契约上应当一致。
